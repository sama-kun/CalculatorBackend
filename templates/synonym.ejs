<!DOCTYPE html>
<html>
<head>
  <title>PDF Report</title>
</head>
<body>
  <h1><%= word %></h1>
  <% for (let i = 0; i < wipo.length; i++) { %>
    <p>Класс <%= wipo[i].cls %>:</p>
  <% for (let i = 0; i < wipo[i].text.length; i++) { %>
    
    <p><%= wipo[i].text[i].charAt(0).toUpperCase() + wipo[i].text[i].slice(1) %>.</p>
  <% } %>

    <br/>
  <% } %>

  <h2>Синонимы:</h2>
  <p>
    <%= synonyms.join(', ') %>
  </p>
</body>
</html>
